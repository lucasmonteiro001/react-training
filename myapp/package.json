{
  "name": "myapp",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "0.8.4"
  },
  "dependencies": {
    "mocha-tap-reporter": "^0.1.3",
    "react": "^15.4.1",
    "react-dom": "^15.4.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "test-mocha": " find ./src -name 'test.js' | xargs mocha --reporter mocha-tap-reporter",
    "eject": "react-scripts eject"
  }
}
